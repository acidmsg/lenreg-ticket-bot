# lenreg_ticket_bot

## Настройка

Для работы бота необходимо создать файл `.env` на основе `.env.example` и заполнить в нем необходимые переменные окружения:

- `BOT_TOKEN`: токен вашего бота
- `PROXY_URL`: URL прокси (опционально)
