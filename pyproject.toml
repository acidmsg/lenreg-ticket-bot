[tool.ruff]
src = ["src"]
target-version = "py314"
exclude = [".venv", "data", "logs"]

[tool.ruff.lint]
# Те самые правила, которые вы хотели: ошибки, импорты, асинхронность, именование
select = ["E", "F", "I", "W", "ASYNC", "N"]

[tool.ruff.format]
quote-style = "double"
indent-style = "space"
