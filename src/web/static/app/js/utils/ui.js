(function () {
  'use strict';

  window.AppUtils = window.AppUtils || {};

  /**
   * Рендерит состояние ошибки с кнопкой «Повторить».
   * Будет реализован в Фазе 4.
   */
  window.AppUtils.renderErrorState = null;

  /**
   * Рендерит пустое состояние (нет данных).
   * Будет реализован в Фазе 4.
   */
  window.AppUtils.renderEmptyState = null;
})();
