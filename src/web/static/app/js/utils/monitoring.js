(function () {
  'use strict';

  window.AppUtils = window.AppUtils || {};

  /**
   * Обработчик кнопки «Обновить» для проверки слотов мониторинга.
   * Будет реализован в Фазе 4.
   */
  window.AppUtils.checkSlots = null;

  /**
   * Обработчик удаления записи мониторинга.
   * Будет реализован в Фазе 4.
   */
  window.AppUtils.deleteMonitoringEntry = null;
})();
