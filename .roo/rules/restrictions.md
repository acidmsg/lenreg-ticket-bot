# Ограничения и запреты

- Игнорировать папки `.git`, `node_modules`, `dist`.
- Соблюдать правила из [`.roo/rules/ignore.md`](ignore.md) — не тратить токены на `.venv/`, `.history/`, `logs/`, `data/`, `__pycache__/` и исторические сессионные логи.
